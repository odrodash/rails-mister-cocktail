<%= simple_form_for @cocktails do |f| %>
  <%= f.input :name, label: 'Name', class: 'text-danger'%>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.button :submit, class: 'btn btn-danger' %>
<% end %>
