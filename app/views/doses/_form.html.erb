<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, label: 'Description' %>
  <%= f.input :ingredient_id, label: 'Ingredient', collection: @ingredients %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>

